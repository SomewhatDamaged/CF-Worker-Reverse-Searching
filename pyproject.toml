[project]
name = "reverse_search"
version = "0.1.0"
description = "Reverse search endpoints for various providers."
requires-python = ">=3.13"
dependencies = [
    "aiohttp",
]

[dependency-groups]
dev = [
]