[project]
name = "Reverse Search"
version = "0.1.0"
description = "Reverse search endpoints for various providers."
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pillow"
]

[dependency-groups]
dev = [
	"workers-py",
	"workers-runtime-sdk"
]